services:
  mongo:
    image: mongo:7
    container_name: atellier-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: atellier_studio

volumes:
  mongo_data:
