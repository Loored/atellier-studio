#!/bin/sh
set -eu

SCRIPT_DIRECTORY=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)

if ! command -v node >/dev/null 2>&1; then
  echo "[atellier-local] Node is unavailable. Install Node 22 and run 'nvm use' in the repository." >&2
  exit 1
fi

exec node "$SCRIPT_DIRECTORY/dev-local.mjs" "$@"
